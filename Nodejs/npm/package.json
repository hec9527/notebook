{
    "name": "npmscript",
    "version": "1.0.0",
    "description": "## 脚本基础",
    "main": "hook.js",
    "scripts": {
        "pretest": "echo \"info: before run test script\"",
        "test": "node hook.js",
        "posttest": "echo \"info: after run test script\"",
        "stop": "echo 'info: stop'",
        "start": "echo 'info: start'",
        "restart": "echo 'info: restart'",
        "open:test": "open http://localhost:9090",
        "serve": "npm i http-server  && http-server -p 9090 dist/",
        "hotload": "live-reload --port 9091 dist/"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "http-server": "^0.12.0"
    }
}
