export {};

declare const query: string;

query.slice();

declare let num: number;

num.toString();

declare type AA = { name: string };

const a: AA = { name: '' };

declare class BB {}

const b = new BB();
